import logo from '../img/logo.png';
const Header = () => {
    return (
        <header className='header'>
            <h1>Todo App <img src={logo} alt="logo" /></h1>
        </header>
    )
}

export default Header
